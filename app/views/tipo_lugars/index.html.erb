<%- model_class = TipoLugar -%>
<div class="page-header">
  <h1><%=t '.title', :default => TipoLugar %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:nombre_tipo) %></th>
      <th><%= model_class.human_attribute_name(:creado_el) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @tipo_lugars.each do |tipo_lugar| %>
      <tr>
        <td><%= tipo_lugar.id %></td>
        <td><%= tipo_lugar.nombre_tipo %></td>
        <td><%=l tipo_lugar.created_at, format: :long %></td>
        <td>
          <%= link_to tipo_lugar_path(tipo_lugar), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to tipo_lugar_path(tipo_lugar), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('Eliminar')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("Nuevo")),
            new_tipo_lugar_path,
            :class => 'btn btn-primary' %>
