<%- model_class = Ciudad -%>
<div class="page-header">
  <h1><%=t '.title', :default => Ciudad %></h1>
</div>

<div class="fieldset">
  <dl>
      <dt class="label label-default"><%= model_class.human_attribute_name(:nombre_ciudad) %></dt>
      <dd><pre class="prettyprint"><%= @ciudad.nombre_ciudad %></pre></dd>
  </dl>
</div>

<div class="form-group">
  <%= link_to t('.back', :default => t("Atrás")),
              ciudads_path, :class => 'btn btn-default'  %>
  <%= link_to t('.edit', :default => t("Editar")),
              edit_ciudad_path(@ciudad), :class => 'btn btn-default' %>
  <%= link_to t('.destroy', :default => t("Eliminar")),
              ciudad_path(@ciudad),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>

